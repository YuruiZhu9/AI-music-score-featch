# AI Guitar Tab Transcriber — Backend Dependencies
# ==================================================
# 生产依赖（必须）:
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.6
pydantic-settings==2.7.1
python-dotenv==1.0.1
httpx==0.28.1
aiofiles==24.1.0
python-magic==0.4.27

# 音频处理（核心）:
librosa==0.11.0
scipy==1.15.1
soundfile==0.12.1
numpy==1.26.4

# MIDI 生成（Guitar Pro / DAW 导入用）:
mido==1.3.3

# PDF 生成:
fpdf2==2.7.9

# 视频 URL 下载（B站 / YouTube / 抖音等）:
yt-dlp==2024.12.6

# ── 可选 GPU 依赖（需要 CUDA 时安装）───────────────────────────────
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
# pip install demucs crepe
# torch==2.5.1
# torchaudio==2.5.1
# demucs==4.1.2
# crepe==0.0.14

# ── Guitar 转谱专用模型（参考 Tabby 项目架构）─────────────────────
# Basic Pitch：Spotify 开源，专为吉他优化，高精度输出 MIDI + Tab
# 集成自 Tabby 项目 https://github.com/JGodbold1/Tabby
basic-pitch>=2024.2.2

# ── 开发 / 测试依赖 ───────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.24.0
